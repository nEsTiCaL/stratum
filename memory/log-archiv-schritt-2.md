# Log-Archiv Schritt 2 (Orchestrator-Kern)

Rotiert aus log.md (P4, 2026-07-03). Aera 2026-06-30: I-2.x, I-D.0/N1-Harness,
Kaltstart-/Sync-Workflow-Entscheidungen, CUDA-/WSL-Funde.

## [2026-06-30] decision | I-2.8 abgeschlossen: Inferenz-Metrik-Erfassung (tok/s). Migration 0003 (model_metrics: id/model/tok_per_s/eval_count/measured_at, Index auf model+measured_at DESC). core/metrics.py: InferenceSample (frozen dataclass), MetricsStore (record/avg_tok_per_s/latest; ORDER BY id DESC fuer Determinismus). OllamaAdapter on_metrics-Callback: Callable[[str, float, int], None] | None; liest eval_count + eval_duration (ns) aus Ollama-Response, berechnet tok/s = eval_count / (eval_duration / 1e9), ruft Callback auf falls beide Felder vorhanden. conftest TRUNCATE um model_metrics erweitert. 10 neue Tests (7x MetricsStore gegen Postgres, 3x Adapter-Callback via MockTransport), 318 gesamt gruen. Grundlage fuer weiche Modellwahl-Profile (schnell/billig, I-5.x).
## [2026-06-30] finding | CUDA PTX-Fehler (GTX1070 + Ollama 0.30.11) durch CUDA_VISIBLE_DEVICES=-1 behoben: Ollama faellt auf Vulkan zurueck, alle Modelle laufen wieder GPU-beschleunigt. Zusaetzlich dokumentiert: num_gpu=0 als Request-Option erzwingt CPU-Inferenz ohne Neustart. Details und curl-Beispiel in portabilitaet.md (Punkt 11) und .local/notes.md.
## [2026-06-30] decision | I-2.7 abgeschlossen: Intent-Zerlegung + Plan-Bestaetigung + DAG-Verkettung (core/planner.py). GoalItem (frozen: task_type/scope/depends_on-tuple), Plan (frozen: goals-tuple/large-bool). IntentDecomposer.decompose(): LLM-Prompt -> JSON-Array -> Plan (LARGE_PLAN_THRESHOLD=5, weiche Warnung). build_dag(): bestaetigte Goals -> verketteter Gesamt-DAG via template_registry.decompose(); Knoten-IDs mit g{i}_-Prefix; Cross-DAG-Kanten: Wurzelknoten (depends_on=()) eines Sub-DAGs erhalten Blatt-IDs der Vorgaenger-Goals. _leaf_ids(): Knoten ohne ausgehende Referenzen = Blaetter. _prefix_dag(): umbenennt alle IDs + Referenzen. 7 neue Tests (4x decompose-Parsing/large-Flag, 3x build_dag-Verkettung via FakeModel+FakeScopeResolver), 308 gesamt. Dev-Verifikation ausstehend (CUDA-Problem). Schritt 2 (Orchestrator-Kern) damit VOLLSTAENDIG.
## [2026-06-30] decision | I-2.6 abgeschlossen: Klassifikation + Detektor-Stub (core/classifier.py). ClassificationResult (frozen dataclass: task_type/complexity/est_input_len/sensitivity/sensitivity_src). _merge_sensitivity(): none<low<high, src=model|detector|both. Classifier.classify() via Model-Seam (JSON-Prompt an phi-4-mini, parst task_type/complexity/est_input_len/sensitivity). _detector_sensitivity() Stub hart none (scharf ab I-3.4). 8 neue Tests (5x _merge_sensitivity det, 3x classify via FakeModel), 301 gesamt. OllamaAdapter-Fix: Body vor raise_for_status lesen (500-Fehlertext, z.B. CUDA-Crash, landet jetzt in RuntimeError statt verloren zu gehen); +1 Test. Dev-Verifikation ausstehend: phi-4-mini crasht mit CUDA PTX-Fehler (Treiberkompatibilitaet); Vulkan-Fallback ueber CUDA_VISIBLE_DEVICES=-1 als moeglicher Workaround dokumentiert.
## [2026-06-30] decision | I-2.5 abgeschlossen: Worker + Model-Seam. OllamaAdapter (core/ollama_adapter.py): POST /api/generate via httpx, ContextExceededError bei "context"-Fehlermeldung, injizierbarer Client fuer Tests. ReplayModel in core/validator.py: prompt-gebundenes Model-Seam-Double (KeyError bei unbekanntem Prompt). DetWorker: ingest_fn-injizierbar (Default: ingest_file), Pfad aus QueueItem.scope. LlmWorker: Router.candidates() + EscalationLoop + put_artifact bei done. WorkerLoop.step(): claim -> det|llm-Verzweigung via TASK_REQUIREMENTS, complete/fail, Exception -> fail+reraise. 14 neue Tests gruen (292 gesamt, make check sauber). Lehre: FakeModel erschoepft bei mehreren Router-Kandidaten (gleiche Instanz per Factory) -> Factory muss fuer alle Nicht-Primär-Kandidaten None zurueckgeben in Tests.
## [2026-06-30] finding | Lint-Annahme korrigiert: ruff/uv laeuft in WSL doch, vorherige Notiz ("uv fehlt im WSL-PATH") war falsch/unvollstaendig. Natives Linux-uv liegt unter ~/.local/bin/uv, fehlt nur im PATH nicht-interaktiver bash -c-Aufrufe -> absoluter Pfad noetig. Falle: gleichnamiges uv.exe ist ueber WSL-Interop aus dem Windows-PATH (winget) erreichbar, ist aber der Windows-Build und beschaedigt/verwirrt das Linux-.venv. portabilitaet.md + .local/notes.md korrigiert. Ruff fand 4 echte F401-Fehler in tests/test_validator.py (behoben); tests/test_queue.py hat 3 vorbestehende F401/Import-Sortier-Fehler aus I-2.3 (nicht angefasst, ausserhalb Scope)
## [2026-06-30] decision | Provenance-Pflichtfeldbefund aus n1-kaltstart.md nach architecture.md (Abschnitt Schema-Vertrag) verschoben -- gehoert dort hin als Schema-Fakt, nicht als Kaltstart-Workflow-Notiz. n1-kaltstart.md behaelt nur die uebergeordnete Lehre (Sub-Modelle wie Provenance per N1 nicht vollstaendig erfassbar, model_json_schema() noetig)
## [2026-06-30] decision | I-2.4 abgeschlossen: Validator + Eskalation (core/validator.py). Validator.validate() verzweigt nach producer_class: det -> ResultDet-Parse ohne Schwelle (Fail = Bug, may_escalate=False, nie Retry/Eskalation); prob -> ResultProb-Parse + CONFIDENCE_THRESHOLDS[task_type] (voll befuellt fuer alle TaskTypes, Default 0.65, Ausnahmen crypto_audit 0.85/document 0.55/explain 0.55), low_confidence/prob_schema_fail/context_exceeded -> may_escalate=True. EscalationLoop.run() laeuft Router.candidates() ab: model_factory(name)->None ueberspringt Kandidat (Cloud pre-S3); prob bekommt 1 Retry am selben Modell bei may_escalate, danach naechster Kandidat; context_exceeded sofort naechster Kandidat ohne Retry; alle erschoepft -> status=unresolved. EscalationOutcome traegt Trace-Felder (status, validation_result, trigger, attempts, final_model, confidence, response). 22 neue Tests (FakeModel als Model-Seam-Doppel, kein echtes Modell), 278 gesamt gruen. Fund: Provenance-Schema (result_det/prob_schema.py) verlangt zusaetzlich timestamp/artifact_type/scope INNERHALB provenance, nicht nur die Top-Level-Felder -> n1-kaltstart.md unvollstaendig, beim Schreiben der Testfixtures per Introspektion nachtraeglich verifiziert und ergaenzt
## [2026-06-30] decision | Sync-Workflow revidiert (mit Nutzer): commit->push->pull pro Testlauf war fuer TDD zu schwerfaellig (Commit-Message-Abstimmung je Rot/Gruen-Schritt). Neu zweiphasig -> portabilitaet.md: Iteration = gezieltes cp einzelner Dateien Windows->WSL (Quelle /mnt/e, Ausfuehrung bleibt ~/stratum-nativ, kein Verstoss gegen kein-mnt-Trick da nicht von dort ausgefuehrt wird), kein Commit pro Schritt; Abnahme = Commit+push AUS WSL (zuletzt getestete Quelle massgeblich), Windows zieht per pull nach. .local/notes.md nachgezogen
## [2026-06-30] decision | N1-Workflow verschaerft: Preflight sofort zu Sessionbeginn (Schritt 2 in arbeitsplan), N1-Queries vor Quelldateien; I-2.4-Befunde (ResultDet/Prob-Felder, Zirkelimport-Check, fehlende Symbole) in n1-kaltstart.md festgehalten
## [2026-06-30] decision | Kaltstart-Regel: kein Quelltext (core/interfaces/tests/queries/) lesen, bevor das konkrete Haeppchen feststeht; nur Memory -> arbeitsplan.md
## [2026-06-30] decision | Sync-Workflow verbindlich: commit->push->git-pull in WSL; kein cp/mnt-Trick; uv-PATH-Hinweis; veralteter Testpfad korrigiert -> portabilitaet.md
## [2026-06-30] decision | I-2.3 abgeschlossen: SQL-Queue (core/queue.py: Queue.enqueue/claim/complete/fail), Migration 0002 (queue-Tabelle, SKIP LOCKED, depends_on jsonb, flags jsonb, CHECK constraint), 15 Tests gruen (256 gesamt). Nebenlaeuflgkeit (SKIP LOCKED, 2 Claimer -> 1 Gewinner) getestet. Abhaengigkeits-Blockade (depends_on) und pre-done-Knoten verifiziert.
## [2026-06-30] finding | N1-Kaltstart-Workflow etabliert: migrate via python -m core.db migrate (nicht yoyo/uv), ingest_file(repo, Path('.'), str(f)), devcli symbol_lookup/index/dependency_map. Spart ~35% Input-Tokens; dient zugleich als Index-Debugging. Klass heisst Repository (nicht StratumRepository). Befunde fuer I-2.3: DagNode-Struktur, connect()-Signatur, template_registry importiert nur stdlib. -> n1-kaltstart.md, arbeitsplan Schritt 5 ergaenzt
## [2026-06-30] finding | Test-Ausfuehrung dieses Setups + Lehre dokumentiert: venv ist Linux -> ueber 'wsl -d Debian' (Default-Distro ist docker-desktop), PYTHONPATH=. .venv/bin/python -m pytest; DB-Tests brauchen laufenden Docker. Symptom "kein Docker-Daemon" hatte die EINFACHE Ursache: Docker Desktop lief nicht (kein Autostart) - System sollte per Preflight vorbereitet sein, einfachste Ursache zuerst pruefen. -> memory/portabilitaet.md
## [2026-06-30] decision | I-2.1 (Capability-Router, Matrix v2) abgeschlossen -> planung/i-2-1-matrix-router.md. Nach Review mit Nutzer das fest-geordnete rank-Listenmodell durch Capability-Modell ersetzt: core/router.py mit MODEL_CAPABILITIES (9 lokal + 8 cloud, Achsen code/reasoning/general 0-100 nach Rubrik) + TASK_REQUIREMENTS (alle 14 task_types -> Achse + [min,max] + Flags; min=Qualitaetsuntergrenze, max=Effizienz-Obergrenze). Router.candidates(task_type, sensitivity, prefs, allowed_models) sortiert (cost_rank, over_band, score): lokal IMMER vor Cloud, in-Band vor Overkill, klein vor gross. Multi-Provider (anthropic/openai/google/groq) + Cloud-Tiers (free/paid_cheap/mid/top); free-Tier (Tageskontingent) vor bezahlt, aber opt-in + nur none/low (Datenschutz: free trainiert ggf. auf Eingaben), Quota-Tracking erst S3. Capacity-Kopplung: lokale Kandidaten gegen allowed_models gefiltert (kehrt die v1-"rein"-Entscheidung bewusst um). exklusive Modelle (q8) nur fuer exclusive-Tasks (crypto). det-Typen -> tree-sitter. CPU-Profil-D faellt aus dem Band (review->Cloud, explain->phi), nicht mehr hartkodiert. MODEL_CONFIG um 4 lokale (14B/32B) erweitert. AENDERT global Cloud=Anthropic (architecture.md/SK7) -> Multi-Provider. det-Linter weiter vertagt. 18 Router- + 12 Capacity-Tests, 205 gesamt; make check gruen. (v1 simple-Matrix nie committed)
## [2026-06-30] decision | I-2.2 (Template-Registry + Task-DAG-Zerlegung) abgeschlossen -> core/template_registry.py. REGISTRY (14 task_types -> NodeTemplate-Tupel): fan-out via scope_rule="files_in" (ScopeResolver-Protocol, vor S4 Dateisystem), Reduce-Knoten (Fan-out-Referenz -> alle expandierten IDs), max_fanout=100, exclusive-Flag (crypto_audit). decompose(task_type, scope, scope_resolver, cache_query, dag_id) materialisiert den DAG; cache_query (scope, artifact_type)->bool klappt gecachte Knoten auf status="done" (kein Worker). DagNode: id/task_type/scope/depends_on/status/flags (frozen). Fan-out-IDs: <template_id>_<i>. 31 Tests gruen, make check gruen (241 gesamt).
## [2026-06-30] finding | I-2.4 (Validator+Eskalation) ist das direkt I-2.1-abhaengige Paket -> Kaltstart-Reibung gesenkt: i-2-1-matrix-router.md neue Sektion "Konsumenten-Vertrag (fuer I-2.4)" (candidates()-Liste = Eskalationspfad, Candidate-Felder, det/prob via producer_class, confidence-Schwelle task_type-abhaengig SK6, Pre-S3-Cloud nicht real -> endet am letzten lokalen Modell, Retry-dann-naechster, Trace-Felder, Model-Seam). arbeitsplan I-2.4-Zeile + inkremente-schritt-2 I-2.4-Block zeigen jetzt auf die Notiz (R2/SK3 = ueberholte rank-Liste, Router ist v2)
## [2026-06-30] decision | Globale Doku auf Multi-Provider-Cloud nachgezogen (Folge I-2.1 Review 2): architecture.md neue Sektion "Cloud-Eskalation: Multi-Provider, abgestuft" (ersetzt Anthropic-only; Tier-Leiter free->paid, Anthropic-Baseline + OpenAI/Google/Gratis opt-in, Auswahl via Capability-Router, IDs/Quota/Adapter = S3); _overview.md Aufbau-Zeile; constraints.md S3-Schicht (opt-in openai/google SDK + Keys); startkonfiguration 7 "Claude-API-Zugang" -> "Cloud-API-Zugang (Multi-Provider)" inkl. Tier-Tabelle + Gratis-Tier-Regeln + Zusammenfassungszeile; inkremente-schritt-3 I-3.1 "Claude-Adapter" -> "Cloud-Adapter (Multi-Provider)"; arbeitsplan I-3.1 nachgezogen. Realer Multi-Adapter-Bau bleibt S3 (I-3.1)
## [2026-06-30] decision | I-2.1 Review 2 (Workflow geklaert): vier Schichten Katalog->eligible(task)->available(host)->ordered(req). Verfuegbarkeit = INSTALLIERT, nicht VRAM-Fit -> Router-Filter allowed_models umbenannt zu installed; Nutzer darf zu grosses Modell fahren (langsam), Capacity beraet statt zu gaten. Neu recommend_install(facts)->InstallPlan: kuratierte Vorschlagsliste je Rolle (general/coding/reasoning) + Begruendung + fits-Flag, Tier nach VRAM (D/A/B/C; D nur vram==0). Konsistente Qualitaet = min-Capability als Versprechen (gleiche Untergrenze ueberall, nur Ort/Kosten variieren); Consistency-Check + konfigurierte-Cloud-Filter VERTAGT auf S3. Cloud-Default = Anthropic-Baseline, Rest opt-in. router importiert capacity (azyklisch). +5 Tests, 210 gesamt; make check gruen; real CPU-Maschine -> tier D (phi lokal, coding/reasoning Cloud)
## [2026-06-30] decision | I-2.0 (Capacity-Profil + Lifecycle) abgeschlossen -> Schritt 2 gestartet -> planung/i-2-0-capacity-lifecycle.md. core/capacity.py: 3 getrennte Ebenen (HardwareFacts gemessen / CapacityPolicy aus capacity.toml / MODEL_CONFIG projektweit), load_policy (tomllib), default_policy (Auto-Detect: GPU 80%-Budget, total_vram=0 -> Profil D), resolve (Startup-Validierung CapacityError bei resident>budget/budget>VRAM/unbekanntem Modell/resident nicht in allowed + Ableitung resident_cost/free/loadable_ondemand/max_parallel), measure_hardware (nvidia-smi + /proc/meminfo hinter Seam, GPU-frei testbar). max_parallel-Heuristik REFERENCE_SLOT_MB=5000 (Startwert, S5 kalibriert) -> Profil A=1/B=2/C=4/D=1. Scope-Grenze: kein Ollama/Swap-Scheduling/Router (I-2.5/2.3/2.1). 12 Tests (reine Logik, kein Postgres), 187 gesamt; make check gruen. Dev-verifiziert: CPU-Maschine -> total_vram 0/ram 7845 -> Profil D. capacity.toml.example committed, capacity.toml gitignored
## [2026-06-30] decision | I-D.0 (Dev-Harness) abgeschlossen -> N1 erreicht (Dogfooding). core/repository.py: SymbolHit + find_symbol(name, *, kind=None) (jsonb-Lateral-Join, deterministisch geordnet). interfaces/devcli/ (neues Paket + interfaces/__init__.py): python -m interfaces.devcli index/symbol_lookup/dependency_map, argparse, --json je Befehl, main(argv, *, repo=None) mit repo-Injektion fuer GPU-/DB-freie Tests, sonst core.db.connect read-only. index/dependency_map absent -> exit 1, symbol_lookup kein Treffer -> exit 0. +15 Tests (6 find_symbol + 9 CLI), 175 gesamt; make check gruen. Dev-verifiziert: core/+interfaces/ (20 Dateien) real in Dev-DB ingestiert, alle Befehle korrekt (symbol_lookup find_symbol -> core/repository.py L167-189). GIN-Index wie entschieden vertagt (S4/N4). -> planung/i-d0-dev-harness.md
## [2026-06-30] decision | I-D.0 (Dev-Harness) Entwurf festgelegt -> planung/i-d0-dev-harness.md: Repository ist Punkt-Lookup, symbol_lookup braucht Quer-Suche -> neue Repository-Methode find_symbol (performante jsonb-Lateral-Abfrage, kein Sprach-/Funktionsausschluss; generische Variante verworfen, widerspraeche dem jsonb-SQL-Rationale); Signatur find_symbol(name, *, kind=None) -> list[SymbolHit] (typisiert, Praefix/Fuzzy aufgeschoben); CLI unter interfaces/devcli/, python -m (kein console_scripts wg. package=false), argparse, menschenlesbar + --json; Schritt-Richtung-Endloesung im Repository-Layer (von I-5.2/Frontends wiederverwendet), graph_edges/CTE (S4) + Go-CLI (P2) NICHT vorgezogen; GIN-Index vertagt (Maszstab N1 + naiver GIN beschleunigt sym->>'name'-Lateral-Join nicht -> S4/N4)
