; call_graph fuer Python, approximativ (I-1.6).
; Die Query findet nur die Aufrufstellen; caller, callee_raw, die heuristische
; Aufloesung (callee_ref) und die Kanten-confidence bestimmt der Extraktor-Kern.
(call) @call
