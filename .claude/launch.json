{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "stratum-dashboard",
      "runtimeExecutable": "wsl",
      "runtimeArgs": ["-d", "Debian", "--", "bash", "-c", "cd ~/stratum && source .venv/bin/activate && uvicorn interfaces.webgui.serve:app --host 0.0.0.0 --port 8000"],
      "port": 8000
    }
  ]
}
