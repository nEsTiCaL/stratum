# Log-Archiv Schritt 3 (Cloud-Bruecke) + Schalen/Memory-Umbau

Rotiert aus log.md (P4, 2026-07-03). Aera 2026-07-01: I-3.x, I-D.2/D.3,
Gedaechtnis-Neuaufbau (Obsidian verworfen), sync.ps1, ops_wsl/docker-Funde.

## [2026-07-01] decision | prob-Task-Fail behoben (Nutzer-gemeldet: fertiger Task -> failed), drei Schichten. (1) serve.py model_factory gab fuer JEDEN Kandidaten einen OllamaAdapter -> Eskalation auf nicht-installierte Modelle (qwen2.5-coder) -> Ollama-404-RuntimeError -> queue.fail(); Fix: OllamaAdapter.list_models() (GET /api/tags) + model_factory gibt None fuer Cloud UND nicht-installierte lokale Modelle (EscalationLoop-Vertrag). (2) Validator/Worker parsten die Modell-Rohausgabe strikt, obwohl kleine Modelle JSON in Markdown-Fences/Prosa verpacken; Fix: core/json_extract.extract_json (dedupliziert die identischen _load_json aus classifier/planner), angewandt in prob-Validator + worker + webgui-submit. (3) KERN-VERTRAG (grep provenance/envelope): phi4-mini liefert einen perfekten Content-Envelope (artifact_type/scope/content/confidence), vermasselt aber den Provenance-Block (kopiert Prompt-Platzhalter producer=gpt-4o-mini, laesst producer_class/artifact_type/scope weg) -> prob_schema_fail. Loesung: das Modell liefert NUR den Envelope, die Provenance stempelt die Producer-Seite. core/provenance_stamp.build_prob_provenance (producer=echtes Modell aus outcome.final_model, input_hash=sha256(Quelle) wie det-Pfad, source_hash=resolve_source_hash); Validator._validate_prob stubt die Provenance beim Validieren (_STUB_PROVENANCE) -> prueft nur den Envelope; Worker + webgui-submit bauen ResultProb aus Envelope + Stempel neu; app.py-Prompt (_make_user_message) fragt keine Provenance mehr ab, SubmitBody.producer default 'manual'. Zusaetzlich: TransientModelError (Ollama-Transportabbruch peer-closed/disconnect -> Retry am SELBEN Modell in EscalationLoop, analog TransientCloudError I-3.1); WorkerLoop.on_item_fail-Callback -> serve.py loggt validation_result/trigger/model/attempts; docker-compose PYTHONUNBUFFERED=1. Live im Container verifiziert: summarize core/scope.py vorher failed, jetzt done, gestempelter input_hash == sha256sum /app/core/scope.py, producer=phi4-mini. 434 Tests gruen (ohne webgui). Test-Harness-Fakten als neuer Chunk `ops_docker-server` festgehalten.
## [2026-07-01] finding | HEAD war entgegen der arbeitsplan-Konvention ("HEAD ist gruen") faktisch rot. (a) Zwei test_queue-Tests (test_fail_back_to_pending, test_fail_increments_attempts) erwarteten noch das ALTE fail()-Verhalten (zurueck auf pending, retrybar), obwohl fail() seit 84ccb3b/6d3c5cd terminal ist (status=failed); auf terminale Semantik nachgezogen (get_status==failed, nicht mehr claimbar, attempts via list_tasks). (b) Lint-Gate rot: app.py 11x E501 (deutsche Prompt-Strings), queue.py B905/UP037, ruff-format-Debt in classifier/planner/cloud_adapter/cost_store + 5 Test-Dateien -> auf Nutzer-Wunsch bereinigt, ruff check . + ruff format --check . jetzt gruen (reine Whitespace/Umbrueche + strict=True/Quotes, keine Logikaenderung). Lehre: Abnahme heisst pytest UND ruff check UND ruff format --check; die HEAD-gruen-Annahme im arbeitsplan deckt Lint nicht ab.
## [2026-07-01] decision | ops_wsl.md aktualisiert: funktionierende WSL+venv+pytest-Aufrufform dokumentiert. Form A (source .venv/bin/activate, bevorzugt) ergaenzt: nach Aktivierung stehen python/pytest/ruff direkt im PATH, kein PYTHONPATH-Prefix noetig (pyproject setzt pythonpath=["."] fuer pytest). Form B (.venv/bin/python) als Schnell-Aufruf behalten. Neu: make-lint-Falle explizit (uv nicht im PATH nicht-interaktiver bash -c -> "uv: No such file", verifiziert); Alternativen: activate&&ruff oder ~/.local/bin/uv. Abschnitt "Was nicht funktioniert" mit verifizierten Nicht-Wegen (.venv\Scripts\python.exe, Windows-Python, /mnt-Ausfuehren). MANIFEST-Zeile nachgezogen.
## [2026-07-01] decision | I-D.2 abgeschlossen: Web-Dashboard (FastAPI+SSE+Claim-Workflow). interfaces/webgui/app.py: create_app(queue, repo, sse_delay, sse_max_events)->FastAPI; GET / (index.html), GET /api/tasks, GET /api/events (SSE), POST /api/claim/{id}, POST /api/submit/{id}. Queue um claim_by_id(item_id, model='human') und list_tasks(statuses) erweitert. Submit-Endpunkt validiert mit Validator(I-2.4)+ResultProb+repo.put_artifact, dann queue.complete. SubmitBody muss auf MODULEBENE (nicht in create_app) definiert sein - from __future__ import annotations macht Annotationen zu Strings, typing.get_type_hints(submit_task) sucht SubmitBody in globalem Namespace (funktioniert nicht bei lokaler Klasse -> FastAPI interpretiert body als Query-Parameter -> 422). sse_delay/sse_max_events in Tests auf 0/3 setzen damit SSE-Generator terminiert. 14 neue Tests (+ 13 Queue-Tests), 437 gesamt gruen, ruff check ausstehend. N3-Dogfooding nutzbar: Browser -> Dashboard, Task uebernehmen, Prompt kopieren, Antwort einreichen.
## [2026-07-01] decision | I-D.3 abgeschlossen: Manual-Adapter (Copy-Paste-Bruecke). core/manual_adapter.py: ManualAdapter(out, inp, sentinel) implementiert dasselbe complete(prompt)->str Interface wie OllamaAdapter/CloudAdapter. display(prompt) deterministisch (gleicher prompt -> gleiche Ausgabe). read_response() liest bis Sentinel "---" oder EOF. Validierungspfad identisch zu API (Validator I-2.4 nimmt den eingefuegten Text, kein Sonderweg). out/inp injizierbar -> testbar ohne echte Konsole. 12 neue Tests, 410 gesamt gruen, ruff clean. N2-Dogfooding ohne VSCode moeglich.
## [2026-07-01] decision | I-3.5 abgeschlossen: Kosten-Telemetrie + Tageskappung. core/cost_store.py: CostStore (record/daily_total, Postgres, Migration 0004.cloud-costs.sql), DailyCostCapError, make_on_cost(store, cap_usd, date_fn) -> (pre_send, on_cost). pre_send() prueft daily_total >= cap -> DailyCostCapError vor jedem API-Call. on_cost(CostRecord) schreibt in DB. CloudAdapter um guard: Callable[[], None] | None = None erweitert (vor _send_with_retry aufgerufen); cloud_model_factory ebenfalls. Analog MetricsStore/on_metrics (I-2.8). 13 neue Tests, 398 gesamt gruen, ruff clean. Schritt 3 (Cloud-Bruecke) VOLLSTAENDIG. N3 erreichbar. Naechstes: I-D.3 Manual-Adapter (Copy-Paste-Bruecke).
## [2026-07-01] decision | I-3.4 abgeschlossen: Detektor-Bibliothek (regex/entropy) + Gate scharf [HARTES GATE]. core/detector.py: DetectionMatch (rule/original/placeholder/sensitivity), DetectorResult (sensitivity/matches/scanner/stub), detect(bytes|str) -> DetectorResult, redact_bytes(bytes, matches) -> bytes. Regex-Patterns fuer: anthropic_api_key, openai_api_key, aws_access_key_id, github_token, google_api_key, jwt (High). Email-Pattern (Low). Shannon-Entropy-Detektion (Schwellwert 4.5 bits/char, 20-200 Zeichen, nur wenn kein High-Match vorher). Deduplizierung ueber seen-Set. core/redaction_gate.py erweitert: scan_real=True-Pfad scannt serialize_bundle()-Bytes mit detect(); High-Matches -> REDACT (Bundle zurueck, redacted_content in Report); keine Matches aber Classifier sensitivity!=none -> BLOCK; sauber -> PASS; stub=False auf Real-Pfad. Alter Stub-Pfad (scan_real=False, unsafe_test_egress) unveraendert (stub=True, alle I-3.3-Tests gruen). core/classifier.py: _detector_sensitivity()-Stub entfernt, nutzt jetzt detect(prompt).sensitivity (scharf). 25 neue Tests (15 test_detector.py: Golden-Parametrize, Clean, Low, Entropy, RedactBytes; 10 test_redaction_gate.py: I-3.4-Sharp-Klasse). 385 gesamt gruen, ruff clean. Realer Cloud-Egress ab jetzt moeglich (Gate scharf, scan_real=True), naechstes I-3.5 Kosten-Telemetrie.
## [2026-07-01] decision | I-3.1 det-core abgeschlossen: Cloud-Adapter (Multi-Provider, Anthropic zuerst; core/cloud_adapter.py). CloudAdapter ist Model-Seam-konform (complete(prompt)->str) und via cloud_model_factory(sender) als LlmWorker/EscalationLoop-model_factory plugbar (None fuer unbekannte/opt-in Namen -> Kandidat uebersprungen wie pre-S3, aber Anthropic-Namen resolven jetzt). Bausteine gegen ReplayCloudSender getestet (KEIN realer Egress, analog ReplayModel I-2.4): resolve_spec (logischer Router-Name->konkrete ID je Anbieter: haiku/sonnet/opus->claude-haiku-4-5/sonnet-4-6/opus-4-8, Preise aus claude-api-Skill; openai/google/groq opt-in->None), compute_cost (in/out + cache-read 0.1x/cache-write 1.25x, CostRecord USD), build_messages (cache_control:ephemeral auf STABILEM Core-Block, byte-identisch bei gleichem cache_prefix unabhaengig vom tail), Retry auf TransientCloudError (max_retries Default 2), Kosten via on_cost-Callback (Muster wie Ollama on_metrics I-2.8), Antwort->ResultProb ueber vorhandenen Validator (I-2.4, kein Duplikat). AnthropicSender = einziger dev-verifizierter Teil, lazy-Import anthropic-SDK (neues opt-in Extra `cloud` in pyproject), adaptive thinking+effort statt budget_tokens; realer Egress erst nach I-3.4 (dev-verif offen). Deferred: OpenAI/Google/Gratis-Backends, Batch, Fast-Mode, free-Quota-Tracking (I-3.5). Konsumenten-Vertrag fuer I-3.5 (on_cost/CostRecord) in spec_schritt-3 ergaenzt. 15 neue Tests, 360 gesamt gruen, ruff clean.
## [2026-07-01] decision | `.local/sync.ps1` zweimal auf Nutzer-Anstoss angepasst: (1) WSL-Sync von `git pull` auf `git fetch origin && git reset --hard @{u}` umgestellt - verwirft lokale WSL-Aenderungen ungefragt statt bei Konflikt (Phase-A-cp-Reste im Index) fehlzuschlagen, da Windows/push ohnehin die massgebliche Quelle ist. (2) Aufruf-Konvention auf absoluten Pfad festgelegt (`<WIN_REPO_PFAD>\.local\sync.ps1`), da das cwd der Nutzer-Konsole nicht garantiert das Repo-Root ist; CLAUDE.md (Commits-Abschnitt) und `ops_sync-workflow` entsprechend praezisiert. sync.ps1-Doku-Kopie in `ops_sync-workflow` synchron gehalten.
## [2026-07-01] decision | I-3.3 abgeschlossen: Redaction-Gate (Stub, Vertrag fix) + fail-safe Egress (core/redaction_gate.py). gate(bundle, sensitivity, policy) -> Decision(PASS|REDACT|BLOCK) + RedactionReport, wiederverwendet die in I-1.8 gebaute EgressPolicy/evaluate_egress-Mechanik aus core/secret_scan.py statt Fail-safe-Logik zu duplizieren (Fund: I-1.8 hatte den Egress-Fail-safe-Teil von I-3.3 bereits vorweggenommen, docstring von evaluate_egress verwies schon auf "R3"). REDACT ist Teil des Decision-Vertrags, vom Stub aber nie erreichbar (kein echter Detektor bis I-3.4). gate() bleibt IO-frei (kein eigener Scan-Aufruf, sensitivity kommt aus Klassifikation I-2.6; kein Trace-Write) - analog zur source_provider-Injektion bei select_hotspots (I-3.2); Trace-Schreiben + BLOCK->unresolved-Verdrahtung sind Aufgabe von I-3.1 (Konsumenten-Vertrag in spec_schritt-3 ergaenzt). 6 neue Tests, 345 gesamt gruen, ruff clean. Naechstes: I-3.1 Cloud-Adapter.
## [2026-07-01] decision | CLAUDE.md Commit-Regel praezisiert: Claude committet nicht mehr selbst per git-Befehlen, sondern gibt nach Bestaetigung der Message nur den Shell-Befehl fuer `.local/sync.ps1 "<message>"`; Nutzer fuehrt ihn selbst in der eigenen Konsole aus (Commit+Push+WSL-Pull in einem Schritt).
## [2026-07-01] decision | Abnahme-Schritte (Commit+Push Windows, WSL git pull) als Script gebuendelt: `.local/sync.ps1`, Aufruf mit Commit-Message als Parameter. Liegt bewusst in .local/ (gitignored, S9), da host-agnostische Logik nur durch WIN_REPO_PFAD/WSL_REPO_PFAD aus .local/host.md parametrisiert wird; Skript-Inhalt deshalb 1:1 in `ops_sync-workflow` dokumentiert, damit es nach einem Klon auf einem neuen Host aus der Doku rekonstruierbar ist statt verloren zu gehen. Kein cp-Mechanismus (das ist Phase-A-Sache), Sync bleibt git pull.
## [2026-07-01] decision | Strukturdatei umbenannt: START.md -> memory_start.md (memory/), .local/notes.md -> .local/host.md. Alle lebenden Verweise nachgezogen (CLAUDE.md, memory-user-guide.md, MANIFEST.md, rules.md, arbeitsplan.md, README.md, ops_wsl/ops_sync-workflow/ops_dogfooding-smoketest); log.md-Historieneintraege bleiben unveraendert (append-only). CLAUDE.md zusaetzlich verschaerft: memory_start.md und .local/host.md werden bei Sitzungsstart direkt gelesen (nicht nur referenziert).
## [2026-07-01] decision | Zwei Nutzer-Anstoesse zur Kaltstart-Geschwindigkeit umgesetzt. (1) ingest_repo() neu in core/ingest.py: ingestiert alle zu `globs` passenden Dateien (Default core/**/*.py + interfaces/**/*.py) in EINEM Aufruf statt Datei-fuer-Datei; source_hash wird einmal aufgeloest (ein git rev-parse statt N Prozessaufrufe), resolve_hash injizierbar fuer Tests. ops_n1-queries-Preflight-Befehl auf ingest_repo umgestellt (Einzeiler statt Glob+Loop-Snippet), gegen echte DB verifiziert (31 Dateien). 4 neue Tests (Sammel-Ingest, Hash-einmal, Overlap-Dedup, deterministische Reihenfolge), 339 gesamt gruen. (2) arbeitsplan.md Kaltstart-Workflow um "Ausgangs-Annahme: HEAD ist gruen" ergaenzt: ein Commit heisst per Konvention volle Suite bestanden, deshalb kein eigener Gruen-Check vor Baubeginn (Schritt 7) noetig, nur N1-Env-Preflight; Suite laeuft ohnehin waehrend TDD und einmal vor der Abnahme (Schritt 8).
## [2026-07-01] decision | Gedaechtnis-Nachtrag zu I-3.2 (Nutzer-Anstoss: welche Quelltext-Reads waeren mit N1 vermeidbar gewesen?): zwei Luecken identifiziert und geschlossen. (1) N1 (`devcli index`) zeigt nur deklarierte Symbole, NICHT die jsonb-Content-Form der drei det-Artefakttypen (Feldnamen wie callee_ref/confidence/span-Format) -> neuer Fakt-Chunk `idx_content-schema` (Tag idx, Quelle core/indexer/{symbols,imports,calls}.py), MANIFEST nachgezogen. (2) Architektur-/Konsumenten-Wissen (wie baut ein Folge-Inkrement auf einem Modul auf) ist grundsaetzlich nicht N1-faehig, gehoert als Prosa ins Gedaechtnis, analog dem I-2.1->I-2.4-Muster -> `spec_schritt-3` bekommt jetzt einen "Konsumenten-Vertrag (fuer I-3.1)"-Abschnitt bei I-3.2 (public API core/bundling.py, Cache-Key-Kandidat). Ziel: I-3.1-Session muss worker.py/template_registry.py/bundling.py nicht erneut vollstaendig lesen.
## [2026-07-01] decision | I-3.2 abgeschlossen: Context-Bundling + deterministische Serialisierung (core/bundling.py). Schritt 3 (Cloud-Bruecke) gestartet. build_core_bundle(repo, scopes) sammelt get_current() je scope fuer symbol_index/dependency_graph/call_graph (fehlende Typen ausgelassen, kein Fehler), scopes sortiert+dedupliziert; module_overview (files/symbol_count) synthetisiert rein aus bereits geladenem symbol_index, keine neue Artefaktart. serialize_core_bundle/serialize_task_context ueber gemeinsames _dump (json.dumps sort_keys+feste separators) -> byte-identisch bei gleichem Inhalt unabhaengig von Einfuege-Reihenfolge. select_hotspots liest call_graph-Kanten mit gesetztem callee_ref (Kandidaten aus dem Substrat, nicht frei gewaehlt), sortiert (scope,span,callee_ref), gekappt auf max_hotspots, Snippet-Extraktion ueber injizierten source_provider(scope)->str (Bundling bleibt I/O-frei testbar, Datei-Lesen ist Aufgabe des Aufrufers). Bundle fasst Core+TaskContext+Hotspots in Sende-Reihenfolge zusammen (serialize_bundle = 3 separat serialisierte, konkatenierte Segmente -> Core-Cache-Hash bleibt bei variablem Anteil unveraendert). Noch nicht verdrahtet: LlmWorker baut Prompts weiterhin direkt aus item.payload (Anschluss ans Bundle folgt vermutlich mit I-3.1 Adapter). 10 neue Tests, 335 gesamt gruen, ruff clean.
## [2026-07-01] decision | Neue Regel rules.md S9: Host-konkrete Werte (absolute Pfade, Laufwerksbuchstaben, IP/Port) NIE in memory/, nur in `.local/notes.md` (gitignored); memory/-Chunks bleiben host-agnostisch (Platzhalter + Verweis). Anlass: `ops_sync-workflow` enthielt einen Windows-Repo-Pfad als Klartext-Wert, der nach einem Repo-Umzug (Laufwerk+Ordner) unbemerkt veraltete. Chunk auf Platzhalter (WIN_REPO_PFAD/WSL_MNT_PFAD) umgestellt, konkrete Werte nach `.local/notes.md` verschoben (neu angelegt, existierte vorher nicht).
## [2026-07-01] finding | Dogfooding-Smoketest scharf gefahren (echter N1-Query gegen frisch indizierten eigenen Code, 30 Dateien, symbol_lookup Repository erfolgreich). Dabei zwei Umgebungsfunde: WSL-Klon war 38 Commits hinter origin/main (Phase-B-Commits laufen aus Windows, WSL-Pull wird bei reiner Windows/Claude-Arbeit leicht vergessen) und der Windows-Repo-Pfad in `ops_sync-workflow` war veraltet (Repo zwischenzeitlich auf anderes Laufwerk/Ordner verschoben, ohne Doku-Nachzug; konkreter Pfad -> `.local/notes.md`, S9). Beide korrigiert; neue Preflight-Checkliste + Dogfooding-Nutzen-Begruendung -> `ops_dogfooding-smoketest`, verlinkt aus `ops_n1-queries`.
## [2026-07-01] finding | Wiederkehrender Fehler: PowerShell-Here-String `@'...'@` im Bash-Tool verwendet -> Delimiter landen woertlich in der Commit-Message (Titel "@"). Zwei Shells im Environment nicht mischen. Sichere Wege (git commit -F <datei> / mehrere -m) + amend-Fix in `ops_sync-workflow` festgehalten.
## [2026-07-01] decision | Gedaechtnis-System neu aufgesetzt (Obsidian verworfen). Von Dataview/Frontmatter/Ordner-Struktur auf claude-optimiertes Retrieval umgestellt: flach, kein Frontmatter, Dateiname <tag>_<slug>.md (Tag fuehrt -> Glob-Cluster). Neue Strukturdateien START.md (Routing), MANIFEST.md (Tag-Registry + Chunk-Index), rules.md (nummerierter Katalog Z/S/F/P, erweiterbar), ersetzt INDEX.md/_index.md/memory-rules.md. 30 Dateien migriert (fm-Strip + [[wikilink]] -> Backtick-Namen scriptgestuetzt). Splits: n1-kaltstart -> ops_n1-queries (dauerhaft) + verworfene Einmal-Befunde; portabilitaet -> env_portabilitaet + ops_sync-workflow; WSL-Aufruf single-sourced in ops_wsl (war 5x redundant). CLAUDE.md zeigt auf START.md. Achsen adressiert: Top-Down (START->MANIFEST), Auffindbarkeit (grep-first + Glob-Cluster + Manifest). arch_core/env_core/plan_core/idx_core bleiben vorerst kohaerente Hubs (Atomisierung = moegliche Phase 2).
